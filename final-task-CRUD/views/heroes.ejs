<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hero List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">

</head>
<body>
    
      <div class="container pt-3">
        <% if( messages.success) { %>
          <div class="alert alert-success" role="alert">
            <%= messages.success %>
          </div>
            <% } %>
        <% if( messages.error) { %>
          <div class="alert alert-danger" role="alert">
            <%= messages.error %>
          </div>
            <% } %>
            <div class="card">
              <div class="card-header bg-primary text-white">
                <h2>My Hero</h2>
              </div>

              <div class="card-body">
                <a href="/heroes/add" class="btn btn-primary mb-3">Add Hero</a>
                <a href="/heroes/add2" class="btn btn-primary mb-3">Add Type</a>
                <div class="container"></div>
                <% for(i = 0; i < data.length; i++) {  %>
                
                  <div class="row">
                  <div class="col-md-3 col-sm-6">
                <div class="card" style="width: 18rem;">
                  <img src="..." class="card-img-top" alt="...">
                  <div class="card-block">
                    <h5 class="card-title">Hero</h5>
                    <p class="card-text">No: <%= i + 1 %> Name: <%= data[i].name %> Type: <%=data[i].type_id %>
                    </p>
                    <a href="/heroes/edit/<%= data[i].id %> " class="btn btn-success btn-sm" class="btn btn-success">Edit</a>
                    <a onclick="return confirm('are you sure want to delete this item?')" 
                    href="/heroes/delete/<%= data[i].id %> " class="btn btn-danger btn-sm ml-3" class="btn btn-danger">Delete</a>
                  </div>
                </div>
              </div>
            </div>
              <% } %> 
              </div>

            
        </div>
        </div>




</body>
</html>